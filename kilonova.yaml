product: pce
component: cluster-ssl
asset: app
version: "4.0"
builder:
  name: docker
  version: "1"
  useContentsForFinalImage: true
deployment:
  helmNamespace: "default"
notifications:
  slackChannel: pce-alerts-low
