apiVersion: batch/v1
kind: Job
metadata:
  name: {{ template "cluster-ssl-app.fullname" . }}-update-certificates
  labels:
    app: {{ template "cluster-ssl-app.name" . }}
    chart: {{ template "cluster-ssl-app.chart" . }}
    release: {{ .Release.Name }}
spec:
  parallelism: 1
  completions: 1
  activeDeadlineSeconds: 600
  template:
    metadata:
      labels:
        app: {{ template "cluster-ssl-app.fullname" . }}
        release: {{ .Release.Name }}
    spec:
      nodeSelector:
        gravitational.io/k8s-role: master
      restartPolicy: OnFailure
      serviceAccountName: {{ template "cluster-ssl-app.serviceAccountName" . }}
      containers:
        - name: update-certs
          image: "{{ .Values.registry }}{{ .Values.image.repository }}:{{ .Values.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: ["update"]
