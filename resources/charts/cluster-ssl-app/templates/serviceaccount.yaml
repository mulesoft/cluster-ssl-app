{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "cluster-ssl-app.serviceAccountName" . }}
  labels:
    app: {{ template "cluster-ssl-app.name" . }}
    chart: {{ template "cluster-ssl-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "cluster-ssl-app.serviceAccountName" . }}
  namespace: monitoring
  labels:
    app: {{ template "cluster-ssl-app.name" . }}
    chart: {{ template "cluster-ssl-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
{{- end -}}
